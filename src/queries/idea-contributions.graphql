query IdeaContributions($ideaId: String!) {
    ideaContributions(where: { idea: $ideaId}, orderBy: contribution, orderDirection: desc) {
        funder {
            id
            profile
        }
        contribution
    }
}