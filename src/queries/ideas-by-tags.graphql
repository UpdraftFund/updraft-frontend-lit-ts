query IdeasByTags($first: Int = 5, $skip: Int = 0, $tags: [String!]) {
    ideas(where: { tags_contains: $tags }, first: $first, skip: $skip, orderBy: shares, orderDirection: desc) {
        ...IdeaFields
    }
}