query SolutionContributionsByFunder($solutionId: String!, $userId: String!) {
    solutionContributions(where: { solution: $solutionId, funder: $userId }) {
        positionIndex
        contribution
        refunded
    }
}