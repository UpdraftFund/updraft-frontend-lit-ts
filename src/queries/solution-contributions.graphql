query SolutionContributions($solutionId: Bytes!) {
    solutionContributions(where: { solution: $solutionId}, orderBy: contribution, orderDirection: desc) {
        funder {
            id
            profile
        }
        contribution
    }
}