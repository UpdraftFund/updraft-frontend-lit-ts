# `now` is seconds since the epoch and used to filter Solutions where the deadline already passed.

query SolutionsByDeadline($now: BigInt) {
    solutions(where: { deadline_gt: $now }, orderBy: stake, orderDirection: desc) {
        ...SolutionFieldsWithIdea
    }
}