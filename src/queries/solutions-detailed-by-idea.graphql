query SolutionsDetailedByIdea($first: Int = 3, $skip: Int = 0, $ideaId: String!) {
    solutions(first: $first, skip: $skip, where: { idea: $ideaId }, orderBy: shares, orderDirection: desc) {
        ...SolutionFieldsDetailed
    }
}
